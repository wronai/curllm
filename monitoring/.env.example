# Monitoring configuration (.env.example)
# Copy to .env and adjust values

# Email recipient for reports
MAIL_TO=you@example.com

# SMTP configuration
SMTP_HOST=smtp.yourmail.com
SMTP_PORT=587
SMTP_USER=apikey_or_user
SMTP_PASS=your_token_or_password
SMTP_SSL=false
MAIL_FROM=monitor@yourhost.local

# Monitoring settings
# Regex to detect issues in page title or signals
ISSUE_PATTERNS="error|not found|forbidden|access denied|bad gateway|service unavailable|502|503|504|timeout|captcha|verification"

# Optional: override input CSV file (one URL per line)
CSV_FILE=

# Optional: extra args passed to curllm (e.g., --stealth --headless)
CURLLM_ARGS=--headless

# Optional: cron schedule expression (default: every 3 hours)
# Examples: daily at 06:00 => CRON_EXPR="0 6 * * *"
CRON_EXPR=

# Binaries (override if needed)
CURLLM_BIN=curllm
JQ_BIN=jq

# Reporting options
# Set DRY_RUN=true to avoid sending emails (report is printed)
DRY_RUN=

# Generate combined HTML report and attach to email
HTML_REPORT=true
# Output path (optional). Default: /tmp/curllm_monitor_report-<timestamp>.html
HTML_REPORT_PATH=

# Slack webhook (optional)
# Example: https://hooks.slack.com/services/T000/B000/XXXX
SLACK_WEBHOOK_URL=

# Telegram (optional)
# Create a bot with @BotFather and get your CHAT_ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
